# Optional: override auto-detected dev server URL
# Use your computer's LAN IP when testing on a physical phone
# EXPO_PUBLIC_API_URL=http://192.168.1.5:3001

# Production web export + deploy-web.sh (baked into JS at build time):
# EXPO_PUBLIC_API_URL=https://kids.synaboard.com

# EAS Android builds: set same URL via eas-cli env (production + preview environments)
