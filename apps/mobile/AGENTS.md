# Expo SDK 54

This app uses **Expo SDK 54** (`expo ~54.0.0` in `package.json`).

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any Expo / React Native / expo-router code.

Do not use unversioned docs or SDK 55+ APIs.

Repo-wide agent context: [../../AGENTS.md](../../AGENTS.md).
