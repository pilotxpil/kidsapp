# Expo SDK 57

This app uses **Expo SDK 57** (`expo ~57.0.17` in `package.json`).

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any Expo / React Native / expo-router code.

Do not use unversioned docs.

Repo-wide agent context: [../../AGENTS.md](../../AGENTS.md).
